# VITE_API_BASE_URL is intentionally commented out — the Vite proxy (vite.config.ts) handles forwarding to the backend
# VITE_API_BASE_URL=https://localhost:7000
VITE_ENABLE_DEV_LOGIN=false
# Set to "true" only when the SignalR hub is deployed and ready
VITE_ENABLE_SIGNALR=false

